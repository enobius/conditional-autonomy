# B1-1: `validator-result.schema.json`

**Status:** DONE
**Specification:** Spec §10.1; Amendment §4

## Field inventory

`validator_id`, `validator_version`, `status`, `criticality`, `enforceability`, `constraint_ids`, `evidence_refs`, `observed_state_version`, `uncertainty_reason`, `permitted_resolution`, `explanation_code`, `repair_hints`, `timestamp`

## Access-class inventory

| Field | Authorized consumers |
|---|---|
| `validator_id` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `validator_version` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `status` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `criticality` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `enforceability` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `constraint_ids` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `evidence_refs` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `observed_state_version` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `uncertainty_reason` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `permitted_resolution` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `explanation_code` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `repair_hints` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `timestamp` | SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |

## Constraint inventory

- The contract is a closed object. Its `required` array is authoritative;
  nullable fields use explicit `null` unions rather than accidental absence.
- Stable IDs, versions, hashes, normalized times, provenance, evidence, and
  approved enums reference `common.defs.schema.json`.
- Arrays constrain item types; identifier sets that represent scopes or
  references reject duplicates.
- Numeric confidence/score fields are bounded to `[0, 1]`; counts, clocks,
  versions, costs, and latency reject negative values where applicable.
- Contract-specific conditionals and access restrictions are encoded directly
  in the schema.
- Required-field/type, enum, pattern/reference, bound, closed-object, and
  conditional failures are treated as equivalence classes. The package-level
  shared-definition corpus covers the referenced primitives; the ticket
  fixtures cover the contract-specific classes.

## Fixtures and validation

- Valid: `fixtures/valid/validator-result.valid.json`
- Invalid: `fixtures/invalid/validator-result.hard-unknown-execute.invalid.json`, `fixtures/invalid/validator-result.pass-repair.invalid.json`
- Both pinned validators are exercised by the package checker.

## Deferred invariants

No new deferred invariant was required. Applicable cross-artifact, temporal,
authorization, integrity, or semantic requirements are already owned by
`deferred-invariants.md`.

## Review

Independent specification-fidelity review completed. See
`outputs/B1_B2_Specification_Fidelity_Review.md`.
