# B2-1: `action-proposal.schema.json`

**Status:** DONE
**Specification:** Spec §§2.3–2.4; Amendment §5

## Field inventory

`action_id`, `plan_id`, `actor_id`, `state_version`, `action_type`, `arguments`, `expected_preconditions`, `expected_effects`, `required_capabilities`, `risk_class`, `reversibility`, `compensation_action`, `postconditions`, `audit_deadline`, `evidence_refs`, `confidence`, `idempotency_key`

## Access-class inventory

| Field | Authorized consumers |
|---|---|
| `action_id` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `plan_id` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `actor_id` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `state_version` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `action_type` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `arguments` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `expected_preconditions` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `expected_effects` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `required_capabilities` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `risk_class` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `reversibility` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `compensation_action` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `postconditions` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `audit_deadline` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `evidence_refs` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `confidence` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |
| `idempotency_key` | POLICY_VISIBLE, SUPERVISOR_VISIBLE, VALIDATOR_VISIBLE, EVALUATOR_ONLY |

## Constraint inventory

- The contract is a closed object. Its `required` array is authoritative;
  nullable fields use explicit `null` unions rather than accidental absence.
- Stable IDs, versions, hashes, normalized times, provenance, evidence, and
  approved enums reference `common.defs.schema.json`.
- Arrays constrain item types; identifier sets that represent scopes or
  references reject duplicates.
- Numeric confidence/score fields are bounded to `[0, 1]`; counts, clocks,
  versions, costs, and latency reject negative values where applicable.
- Contract-specific conditionals and access restrictions are encoded directly
  in the schema.
- Required-field/type, enum, pattern/reference, bound, closed-object, and
  conditional failures are treated as equivalence classes. The package-level
  shared-definition corpus covers the referenced primitives; the ticket
  fixtures cover the contract-specific classes.

## Fixtures and validation

- Valid: `fixtures/valid/action-proposal.valid.json`
- Invalid: `fixtures/invalid/action-proposal.compensation-null.invalid.json`, `fixtures/invalid/action-proposal.audit-null.invalid.json`, `fixtures/invalid/action-proposal.irreversible-compensation.invalid.json`
- Both pinned validators are exercised by the package checker.

## Deferred invariants

No new deferred invariant was required. Applicable cross-artifact, temporal,
authorization, integrity, or semantic requirements are already owned by
`deferred-invariants.md`.

## Review

Independent specification-fidelity review completed. See
`outputs/B1_B2_Specification_Fidelity_Review.md`.
